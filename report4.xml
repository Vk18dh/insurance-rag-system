<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="0" failures="1" skipped="0" tests="8" time="0.434" timestamp="2026-07-13T22:19:09.125762+05:30" hostname="Dhyan"><testcase classname="phase2.tests.test_risk_services" name="test_ambiguity_detector_happy_path" time="0.002" /><testcase classname="phase2.tests.test_risk_services" name="test_ambiguity_detector_empty_payload" time="0.001" /><testcase classname="phase2.tests.test_risk_services" name="test_legal_sensitivity_checker_happy_path" time="0.000" /><testcase classname="phase2.tests.test_risk_services" name="test_exclusion_checker" time="0.000" /><testcase classname="phase2.tests.test_risk_services" name="test_regulatory_checker" time="0.000" /><testcase classname="phase2.tests.test_risk_services" name="test_escalation_service_high_risk" time="0.000" /><testcase classname="phase2.tests.test_risk_services" name="test_escalation_service_low_risk" time="0.000" /><testcase classname="phase2.tests.test_risk_agent" name="test_risk_agent_integration" time="0.004"><failure message="phase2.exceptions.risk_exception.RiskException: Pipeline crashed organically generating factors seamlessly: 1 validation error for RiskAssessmentResult&#10;reasoning_source&#10;  Input should be a valid dictionary or instance of ReasoningResult [type=model_type, input_value=&lt;MagicMock id='1852248691040'&gt;, input_type=MagicMock]&#10;    For further information visit https://errors.pydantic.dev/2.12/v/model_type">self = &lt;phase2.services.risk_assessment_service.RiskAssessmentService object at 0x000001AF42A03A10&gt;
reasoning_result = &lt;MagicMock id='1852248691040'&gt;

    def assess_risk(self, reasoning_result: ReasoningResult) -&gt; RiskAssessmentResult:
        start_ms = time.perf_counter()
    
        try:
            # 1. Format payload completely free from unverified documents cleanly.
            query_text = getattr(reasoning_result.verification_source.retrieval_result.query_context, "original_query", "Unknown Context")
    
            # Map reasoning string constraints out cleanly.
            logic_summary = ""
            for step in reasoning_result.reasoning_chain.steps:
                logic_summary += f"[Step {step.step_number}]: {step.conclusion}\n"
    
            prompt = self.prompt_template.format(
                QUERY=query_text,
                REASONING_BLOCK=logic_summary
            )
    
            # 2. Singular &lt; 1s LLM evaluation across 5 metrics securely.
            unified_risk_payload = self.llm.analyse(prompt)
    
            # 3. Synchronous Dictionary Dispatch parsing logic securely mapped.
            ambiguity_report = self.ambiguity_detector.detect(unified_risk_payload)
            legal_report = self.legal_checker.check(unified_risk_payload)
            exclusion_report = self.exclusion_checker.check(unified_risk_payload)
            regulatory_report = self.regulatory_checker.check(unified_risk_payload)
            escalation_report = self.escalation_service.evaluate(unified_risk_payload)
    
            # Count factors mapped truly natively.
            factors_detected = sum([
                1 if ambiguity_report.detected else 0,
                1 if legal_report.detected else 0,
                1 if exclusion_report.detected else 0,
                1 if regulatory_report.detected else 0
            ])
    
            # Parse overall Risk Level from High_risk
            high_risk_root = unified_risk_payload.get("high_risk", {})
            r_str = high_risk_root.get("risk_level", "LOW")
            try:
                ov_risk = RiskLevel(r_str)
            except ValueError:
                ov_risk = RiskLevel.LOW
    
            end_ms = time.perf_counter()
            exec_time = (end_ms - start_ms) * 1000.0
    
            logger.info(
                "Risk Analysis generation complete",
                extra={
                    "total_ms": exec_time,
                    "factors_detected": factors_detected,
                    "escalation": escalation_report.escalation_required
                }
            )
    
&gt;           return RiskAssessmentResult(
                reasoning_source=reasoning_result,
                overall_risk_level=ov_risk,
                ambiguity_report=ambiguity_report,
                legal_warnings=legal_report,
                regulatory_warnings=regulatory_report,
                exclusions_identified=exclusion_report,
                escalation=escalation_report,
                metrics=RiskProcessingMetrics(
                    execution_time_ms=exec_time,
                    risk_factors_detected=factors_detected
                )
            )
E           pydantic_core._pydantic_core.ValidationError: 1 validation error for RiskAssessmentResult
E           reasoning_source
E             Input should be a valid dictionary or instance of ReasoningResult [type=model_type, input_value=&lt;MagicMock id='1852248691040'&gt;, input_type=MagicMock]
E               For further information visit https://errors.pydantic.dev/2.12/v/model_type

phase2\services\risk_assessment_service.py:109: ValidationError

During handling of the above exception, another exception occurred:

mock_reasoning_result = &lt;MagicMock id='1852248691040'&gt;
mock_llm_analyzer = &lt;Mock spec='ILLMAnalyzer' id='1852248700112'&gt;
monkeypatch = &lt;_pytest.monkeypatch.MonkeyPatch object at 0x000001AF42973E10&gt;

    def test_risk_agent_integration(mock_reasoning_result, mock_llm_analyzer, monkeypatch):
        # Avoid reading the actual file on disk during tests if it doesn't exist.
        # We patch open directly, or we can just patch `_load_template`.
        from phase2.services.risk_assessment_service import RiskAssessmentService
    
        # We create the factory manually or patch the file reading.
        monkeypatch.setattr(RiskAssessmentService, "_load_template", lambda self, path: "Mock Template {QUERY} {REASONING_BLOCK}")
    
        settings = MagicMock()
        settings.risk.prompt_template_path = "mock"
        settings.risk.supported_risk_categories = []
        agent = RiskAgentFactory.create(settings, mock_llm_analyzer)
    
&gt;       result = agent.evaluate(mock_reasoning_result)
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

phase2\tests\test_risk_agent.py:63: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
phase2\agents\risk_agent.py:33: in evaluate
    risk_result = self._assessment_service.assess_risk(reasoning_result)
                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = &lt;phase2.services.risk_assessment_service.RiskAssessmentService object at 0x000001AF42A03A10&gt;
reasoning_result = &lt;MagicMock id='1852248691040'&gt;

    def assess_risk(self, reasoning_result: ReasoningResult) -&gt; RiskAssessmentResult:
        start_ms = time.perf_counter()
    
        try:
            # 1. Format payload completely free from unverified documents cleanly.
            query_text = getattr(reasoning_result.verification_source.retrieval_result.query_context, "original_query", "Unknown Context")
    
            # Map reasoning string constraints out cleanly.
            logic_summary = ""
            for step in reasoning_result.reasoning_chain.steps:
                logic_summary += f"[Step {step.step_number}]: {step.conclusion}\n"
    
            prompt = self.prompt_template.format(
                QUERY=query_text,
                REASONING_BLOCK=logic_summary
            )
    
            # 2. Singular &lt; 1s LLM evaluation across 5 metrics securely.
            unified_risk_payload = self.llm.analyse(prompt)
    
            # 3. Synchronous Dictionary Dispatch parsing logic securely mapped.
            ambiguity_report = self.ambiguity_detector.detect(unified_risk_payload)
            legal_report = self.legal_checker.check(unified_risk_payload)
            exclusion_report = self.exclusion_checker.check(unified_risk_payload)
            regulatory_report = self.regulatory_checker.check(unified_risk_payload)
            escalation_report = self.escalation_service.evaluate(unified_risk_payload)
    
            # Count factors mapped truly natively.
            factors_detected = sum([
                1 if ambiguity_report.detected else 0,
                1 if legal_report.detected else 0,
                1 if exclusion_report.detected else 0,
                1 if regulatory_report.detected else 0
            ])
    
            # Parse overall Risk Level from High_risk
            high_risk_root = unified_risk_payload.get("high_risk", {})
            r_str = high_risk_root.get("risk_level", "LOW")
            try:
                ov_risk = RiskLevel(r_str)
            except ValueError:
                ov_risk = RiskLevel.LOW
    
            end_ms = time.perf_counter()
            exec_time = (end_ms - start_ms) * 1000.0
    
            logger.info(
                "Risk Analysis generation complete",
                extra={
                    "total_ms": exec_time,
                    "factors_detected": factors_detected,
                    "escalation": escalation_report.escalation_required
                }
            )
    
            return RiskAssessmentResult(
                reasoning_source=reasoning_result,
                overall_risk_level=ov_risk,
                ambiguity_report=ambiguity_report,
                legal_warnings=legal_report,
                regulatory_warnings=regulatory_report,
                exclusions_identified=exclusion_report,
                escalation=escalation_report,
                metrics=RiskProcessingMetrics(
                    execution_time_ms=exec_time,
                    risk_factors_detected=factors_detected
                )
            )
    
        except Exception as e:
            logger.exception(f"Fatal logic crash within Risk Execution natively: {e}")
&gt;           raise RiskException(f"Pipeline crashed organically generating factors seamlessly: {e}")
E           phase2.exceptions.risk_exception.RiskException: Pipeline crashed organically generating factors seamlessly: 1 validation error for RiskAssessmentResult
E           reasoning_source
E             Input should be a valid dictionary or instance of ReasoningResult [type=model_type, input_value=&lt;MagicMock id='1852248691040'&gt;, input_type=MagicMock]
E               For further information visit https://errors.pydantic.dev/2.12/v/model_type

phase2\services\risk_assessment_service.py:125: RiskException</failure></testcase></testsuite></testsuites>